# 博客公开内容边界

- `content/` 是独立真实目录，其中所有文章和附件均视为可公开；`publish` 和 tag 不控制权限。
- 不连接知识库、不按标记同步、不递归导入目录；使用 `scripts/copy-content.py` 显式列出文件，先预览差异再加 `--apply`。
- 引用其他笔记不代表获准复制。附件逐项审阅；当前允许 Markdown 和常见栅格图片，PDF 等需单独审阅并修改允许规则。
- 不把原稿绝对路径、内部索引、研究材料或复制日志放入公开目录。
- 更新副本保留正文表达，先核对已有副本差异；知识库改动不会自动同步。
- 正式构建使用 `npm run build`，发布使用 `./deploy.sh`；发布前检查 `public`。禁止直接上传旧产物或跳过内容检查。
- 所有本地发布副本纳入 Git；`.release`、旧产物备份、`public` 不提交。撤下文件后重新构建，另行检查线上及历史副本。
- `quartz/static` 也属于公开输入，受 static-manifest.json 的逐文件哈希约束；变更必须先审阅。构建产物须通过 check-release.py，不能仅靠哈希回执判定安全。
